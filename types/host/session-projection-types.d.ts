/**
 * Ambient merge target for session-projection keys (upstream
 * `@deepseek-ai/dsh-session-projection/types`). The `redteam` key is declared
 * in src/types.ts via module augmentation of this interface.
 */

export interface SessionProjectionStateMap {}
